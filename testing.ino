// TESTING

void test() {
  // Write testing code here!
  // Serial.print(findIndexOf('j', characters, sizeof(characters)));
  // Serial.println(findIndexOf( ".-", patterns, sizeof(patterns) / sizeof(patterns[0])));
  // Serial.println(patternToChar("-----"));
  // Serial.println(digitalRead(BUTTONPIN));
  charToLED('0');
}
